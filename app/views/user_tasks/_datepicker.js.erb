$(document).ready(function() {
  $('#user_task_due').datepicker({
    autoclose: true,
    todayHighlight: true,
    toggleActive: true,
    format: "mm-dd-yyyy",
    orientation: "bottom auto"
  });
});
